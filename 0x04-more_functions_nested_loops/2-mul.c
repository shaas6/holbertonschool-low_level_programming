#include "holberton.h"

/**
 * mul - multiply two integers together
 * @result - result of the multiplication string
 * @a: variable being multiplied
 * @b: second variable being multiplied
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
