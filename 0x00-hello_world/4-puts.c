#include <stdio.h>
#include <string.h>

/**
 * main - Entry point, puts command
 *
 * Return: 0, success
 */
int main(void)
{
	char str1[15];

	strcpy(str1, "\"Programming is like building a multilingual puzzle");

	puts(str1);

	return(0);
}
